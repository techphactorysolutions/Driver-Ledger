{
  "name": "driveledger-pwa",
  "version": "3.7.3",
  "private": true,
  "description": "DriveLedger repaired after Claude package review: restored missing tests/tools/_redirects, retained public security audit, modern UI, screenshot-first quick add, OCR review, local-first analytics, backups, and PWA packaging.",
  "scripts": {
    "test": "python -m unittest discover -s tests",
    "syntax": "node --check app.js && node --check service-worker.js",
    "smoke": "node tools/smoke-startup.js"
  },
  "dependencies": {},
  "devDependencies": {}
}
