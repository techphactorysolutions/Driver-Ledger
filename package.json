{
  "name": "giglens-pwa",
  "version": "4.0.0",
  "private": true,
  "description": "GigLens premium local-first multi-app driver command center with screenshot-assisted earnings capture, mileage, profit, tax, platform and zone analytics, paused shifts, and order decisions.",
  "scripts": {
    "test": "python -m unittest discover -s tests",
    "syntax": "node --check app.js && node --check service-worker.js",
    "smoke": "node tools/smoke-startup.js"
  },
  "dependencies": {},
  "devDependencies": {}
}
